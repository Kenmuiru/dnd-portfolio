# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dnd-portfolio?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (Change this to a random string!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Credentials (Default - change after first login)
ADMIN_USERNAME=admin
# Password will be hashed in the database, default is: admin123

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
